# Plan

1. 先新增页面结构红测，保留当前脏工作树的索引工作。
2. 在现有原生HTML/CSS/JS栈内分离导航与业务视图，使用原生dialog建立详情抽屉，避免不必要框架迁移与依赖。
3. 完善任务入口、渐进筛选、批量工具、未保存提示、连接重试、窄屏与键盘操作。
4. 运行npm run check、npm test、git diff --check，浏览器验证，独立新上下文审查，记录未验证项。

Ownership：仅本独立前端public、ui-tests、文档；不修改后端、代理API契约或其他仓库。无新媒体需求，动画强度1，采用克制绿色与系统中文字体。

完成记录：步骤1–4本地完成。全量100项通过；独立复核PASS；真实服务只读桌面/窄屏/历史导航验收。由于后端未启用上传/索引，本次对应写操作仅有自动化证据，未进行真实provider或生产发布。
