# 0004 验证记录 · 2026-09-07

结果：前端本地实现完成，独立复核通过；未提交、推送或生产发布。仅修改独立前端仓库；后端源码、配置、数据、进程与Java内置页面均未修改。

## 行为与证据

- 资料库/处理任务/设置为独立hash视图，支持直接地址、前进后退及未知路由回退；页面标题、当前导航和焦点跟随变化。
- 资料库保留目录、服务器分页/筛选、上传入口和批量管理。筛选默认收起并显示有效筛选数；批量工具仅有选择时展示。详情使用原生dialog侧抽屉，保留元数据/权限/任务入口和编辑。
- 任务页从当前授权资料页读取解析与索引两链，可切类型、查看详情、刷新和操作原有任务。界面明确不包含全部历史。
- 任务导航保留原详情草稿；返回恢复相同表单节点；设置、关闭和用户筛选/翻页丢弃草稿前确认。身份失效、资料撤权仍优先清空。
- 配置与身份表单集中设置页；重新连接可重读配置，不保存凭证。

## 自动化

先新增导航结构与抽屉/渐进工具两个红测：旧页面2/2失败。实现后转绿。
完整回归保留原91项，新增2项结构和7项实际app DOM行为回归；最终 `npm run check`、`npm test` **100/100通过**，`git diff --check`通过。

首次沙箱运行HTTP临时端口受到EPERM限制；重新在允许本机监听的测试环境运行全部用例，无删除、跳过或放宽测试。中间任务列表错误调用缺失任务校验被原详情回归捕获，已加存在检查并重跑整个UI回归。

7项实际交互测试覆盖视图切换关闭/恢复同一草稿，设置取消，筛选取消恢复原值且不发请求，翻页取消，抽屉放弃确认，两类任务授权范围，以及条件批量工具/未知路由回退。

## 浏览器

使用Codex浏览器工具对已存在的本机前端18085及Java18084做只读验收，未启动/停止后端，未提交业务写操作。

- 默认1280像素桌面：真实加载4份既存合成资料；目录、表格、折叠筛选和选中后批量工具可见；未选择时批量工具不占位。
- 打开资料侧抽屉，背景不可操作，焦点进入详情；未保存名称触发离开确认。确认的接受/取消分支另由实际app DOM测试覆盖。
- 三个导航页面独立显示，设置读取真实能力为资料整理；上传按服务能力禁用。
- 390×844窄屏：顶部导航、横向目录、列表主要列与抽屉可用；浏览器后退从资料抽屉到设置会关闭模态层，前进回资料页恢复详情，Escape关闭并回到资料标题。
- 验收标签页console error/warn为空。

独立审查首次提出隐藏草稿丢弃与历史导航模态框残留两项问题；修复后新上下文复核29项导航/详情测试通过，给出PASS（本次代码与自动化范围）。

## 验证边界与偏离

现有Java服务没有开放上传/索引能力。真实上传、取消/重试、索引写操作的本次浏览器联调未验证；原有对应自动化通过，不替代真实provider/Milvus验收。此次不改回答/检索/证据，因此RAG acceptance为不适用，不标记通过。没有生产部署或生产验收。

保留原生HTML/CSS/ES modules，无框架/媒体依赖变更；没有新增代理路由。任务页采用当前授权资料页范围，是现有API边界，不能称全局任务中心。未来全库任务历史和问答页面需后端契约实际开放后独立实现。
