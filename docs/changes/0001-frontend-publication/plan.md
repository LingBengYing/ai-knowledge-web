# Plan

1. 固定 Java 发布基线，按六文件 allowlist 导出静态资源与20项UI回归，保留来源。
2. 先写真实 HTTP 开发代理失败测试，再实现最小 Node Interface；不复制 Java 业务规则。
3. 补完整相对路径文档、配置、安全说明、secret checker 和 Node22 CI。
4. 运行完整测试、语法与源码一致性检查；由未参与实现的上下文独立审查，修复后重跑全量。
5. 负责人核验远端、暂存扫描、历史扫描并正常上传；发布状态和 CI 以实际结果记录。

范围选择：fullstack-dev 指导配置、安全错误和前后端集成；本次只是开发适配器，不新建数据库、业务认证、刷新令牌或生产网关。前端仍使用相对路径，不为分仓而新增跨域凭据。
