# 验证记录 · 2026-09-07

状态：本地文件查看器实现并验证；**库内原文件预览仍未完成**，缺口见backend-integration.md。仅独立前端改动，无Java/Python、数据库、后端配置/进程或Java静态文件修改。

## 自动化

预览模块测试先因缺失实现失败，随后类型、空文件/限额、签名与MIME、文本安全、Blob URL替换释放和异步代次转绿。独立审查额外复现关闭后迟到文件选择导致资源保留，修复入口与异步完成保护；补事件层关闭、身份重置、停止播放和迟到change回归。

最终 `npm run check`、`npm test` **109/109**、`git diff --check`通过。原100项回归保留。前端dev-server仅新增preview.mjs静态白名单及img/media/object的本地blob来源，未新增原文件API路由，Origin/Host/鉴权/预算保持。

独立新上下文复核资源问题已关闭，预览当时6/6通过；随后补充播放停止与真实change事件测试，最终8项预览单测通过。

## 浏览器实际验证

使用当前本机前端与明确合成的临时测试文件，未上传至后端，未关联任何库内记录。

- PNG：显示实际640×360图像及分辨率，点击放大后125%。
- WAV：读取时长3.0秒，点击播放后出现暂停按钮。
- MP4（H.264/AAC）：解码并显示640×360测试画面；点击播放后DOM观察currentTime>0、paused=false、duration=3、error=null。
- Markdown：显示中文与原始script标记，未执行标记。
- PDF：当前Codex内置浏览器不支持object内嵌PDF，显示预设兼容回退及新窗口链接；**未把此结果记作内嵌PDF渲染成功**。浏览器原生PDF能力依环境，未引入第三方文档转换服务。
- 验收浏览器console error/warn为空。媒体类型声明和签名校验不保证所有编解码器，解码错误由播放器提示。

测试素材通过ffmpeg生成视频/音频/图像，并生成最小有效PDF和纯文本，不包含真实业务信息。前端18085开发进程因静态模块和CSP变化按确认的仓库路径重启；后端18084没有重启。

## 未完成项

没有原文件API，因此当前列表的库内图片/音视频仍不能自动预览。本地文件选择是一项独立功能，页面明确“不上传、不代表已读取库内资料”，不是库内预览的替代验收。

RAG acceptance不适用：本轮不读取RAG来源、不生成答案、不修改证据。真实远程原文件、Range、ACL撤销、跨浏览器PDF及生产部署均待后续验收。没有提交/推送或发布生产。
